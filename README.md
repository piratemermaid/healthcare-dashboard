# healthcare-dashboard

## Structure

```
├── client/          # React (Vite) frontend
├── server/          # Node/FastAPI backend
├── docker-compose.yml
└── .env.example
```

## Setup

- **Client:** `cd client && npm install && npm run dev`
- **Server:** `cd server && npm install`
- **Docker:** `docker-compose up`
